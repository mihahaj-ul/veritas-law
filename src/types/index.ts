export type NavLink = {
  label: string;
  href: string;
};

export type PracticeArea = {
  icon: string;
  title: string;
  description: string;
  services: string[];
};

export type Attorney = {
  name: string;
  title: string;
  specialization: string;
  image: string;
  initials: string;
};
